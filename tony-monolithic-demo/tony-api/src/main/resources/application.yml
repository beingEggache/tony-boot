server:
  port: 10000
spring:
  profiles:
    default: dev
  application:
    name: tony-api
  main:
    lazy-initialization: true
  cloud:
    compatibility-verifier:
      enabled: false
    openfeign:
      lazy-attributes-resolution: true
