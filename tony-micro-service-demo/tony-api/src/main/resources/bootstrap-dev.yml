spring:
  cloud:
    nacos:
      username: nacos
      password: Nacos@ktwl
      discovery:
        namespace: "dev"
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
