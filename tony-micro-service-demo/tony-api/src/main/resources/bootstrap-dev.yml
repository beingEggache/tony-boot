spring:
  config:
    import: optional:polaris:DEFAULT_GROUP:db-and-redis.yml
  cloud:
    polaris:
      namespace: "dev"
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    log-prefix: "mybatis"
